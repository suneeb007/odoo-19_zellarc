<?xml version="1.0" encoding="utf-8"?>
<odoo>
  <record id="view_sale_order_form_inherit_opportunity_move" model="ir.ui.view">
    <field name="name">sale.order.form.inherit.opportunity.move</field>
    <field name="model">sale.order</field>
    <field name="inherit_id" ref="sale.view_order_form"/>
    <field name="arch" type="xml">
      <!-- Remove any existing opportunity field placements so we don't show duplicates -->
      <xpath expr="//field[@name='opportunity_id']" position="replace">
        <!-- removed by crm_lead_sequence to move it under the customer header -->
      </xpath>

      <!-- Insert the opportunity selector under the customer (partner) block -->
      <xpath expr="//group[@name='partner_details']//field[@name='partner_id']" position="after">
        <field name="opportunity_id" groups="sales_team.group_sale_salesman" context="{
                    'default_campaign_id': campaign_id,
                    'default_company_id': company_id,
                    'default_medium_id': medium_id,
                    'default_partner_id': partner_id,
                    'default_source_id': source_id,
                    'default_tag_ids': tag_ids,
                    'default_type': 'opportunity',
                }"/>
        <field name="opportunity_id" groups="!sales_team.group_sale_salesman" context="{
                    'default_campaign_id': campaign_id,
                    'default_company_id': company_id,
                    'default_medium_id': medium_id,
                    'default_partner_id': partner_id,
                    'default_source_id': source_id,
                    'default_type': 'opportunity',
                }"/>
      </xpath>
    </field>
  </record>
</odoo>
