<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_open_flight_booking_sale" model="ir.actions.server">
        <field name="name">Open Flight Booking (Sale)</field>
        <field name="model_id" ref="sale.model_sale_order"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
if record and record.flight_booking_id:
    action = {
        'type': 'ir.actions.act_window',
        'res_model': 'flight.booking',
        'res_id': record.flight_booking_id.id,
        'view_mode': 'form',
        'target': 'current',
    }
else:
    action = {'type': 'ir.actions.act_window_close'}
        ]]></field>
    </record>

    <record id="action_open_flight_booking_purchase" model="ir.actions.server">
        <field name="name">Open Flight Booking (Purchase)</field>
        <field name="model_id" ref="purchase.model_purchase_order"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
if record and record.flight_booking_id:
    action = {
        'type': 'ir.actions.act_window',
        'res_model': 'flight.booking',
        'res_id': record.flight_booking_id.id,
        'view_mode': 'form',
        'target': 'current',
    }
else:
    action = {'type': 'ir.actions.act_window_close'}
        ]]></field>
    </record>
</odoo>
