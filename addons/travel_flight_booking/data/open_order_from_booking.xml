<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <record id="action_open_sale_from_booking" model="ir.actions.server">
        <field name="name">Open Sale Order from Booking</field>
        <field name="model_id" ref="travel_flight_booking.model_flight_booking"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
if record and record.sale_order_id:
    action = {
        'type': 'ir.actions.act_window',
        'res_model': 'sale.order',
        'res_id': record.sale_order_id.id,
        'view_mode': 'form',
        'target': 'current',
    }
else:
    action = {'type': 'ir.actions.act_window_close'}
        ]]></field>
    </record>

    <record id="action_open_purchase_from_booking" model="ir.actions.server">
        <field name="name">Open Purchase Order from Booking</field>
        <field name="model_id" ref="travel_flight_booking.model_flight_booking"/>
        <field name="state">code</field>
        <field name="code"><![CDATA[
if record and record.purchase_order_id:
    action = {
        'type': 'ir.actions.act_window',
        'res_model': 'purchase.order',
        'res_id': record.purchase_order_id.id,
        'view_mode': 'form',
        'target': 'current',
    }
else:
    action = {'type': 'ir.actions.act_window_close'}
        ]]></field>
    </record>
</odoo>
